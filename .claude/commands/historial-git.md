---
description: Reescribe fechas y mensajes de un rango de commits del historial git
---

**Este comando no tiene proceso propio: invoca el de essence.** El proceso de reescritura de
historial git vive en `avatar-mind/narrative/[estado-actual]/life-cycle/procesos/` de essence —
life-cycle se lee directamente, nunca se copia.

Proceso: `../019d8dc4-d3e0-76ca-b047-70e2b5b71674/avatar-mind/narrative/[estado-actual]/life-cycle/procesos/historial-git.md`

Lee y ejecuta ese proceso con los argumentos `$ARGUMENTS`.

`$ARGUMENTS` puede traer, en cualquier orden, el rango de commits, la fecha de inicio y la fecha de
fin del rango temporal objetivo (ver § *Parseo de `$ARGUMENTS`* del proceso). Si falta algo, el
proceso lo pregunta en el propio turno.

Regla: no copies este proceso a tu propio `avatar-mind/`. Si algo falta, se arregla en essence.
